machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install -r codalab/requirements/dev_azure_nix.txt

test:
  override:
    - export PYTHONPATH=`pwd`; (cd codalab && python manage.py test)
